# Dependencies
node_modules/

# Environment (never commit secrets)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Uploaded files (from POST /admin/upload/image)
uploads/

# Prisma
/src/generated/prisma

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE / Editor (uncomment if you want to ignore)
.idea/
.vscode/
*.swp
*.swo
*~

# Build / output (if added later)
dist/
build/
out/
.next/

# Test / coverage (if added later)
coverage/
.nyc_output/
*.lcov

# Optional: lock file (only if team uses a single package manager)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
